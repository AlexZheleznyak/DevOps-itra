function lcs(strings) {if (!strings.length) return '';let shortest = strings.reduce((a, b) => a.length <= b.length ? a : b),maxlen = shortest.length;for (let len = maxlen; len > 0; len--) {for (let start = 0; start <= maxlen - len; start++) {let substr = shortest.slice(start, len);if (strings.every(elem => elem.includes(substr)))return substr;}}return '';}console.log(lcs(process.argv.slice(2)));
